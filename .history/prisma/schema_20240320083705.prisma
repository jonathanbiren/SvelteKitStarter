// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

//Tells Prisma which client to generate (in this case, Prisma Client for JavaScript)
generator client {
  provider = "prisma-client-js"
}

//Givs prisma the infor
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
