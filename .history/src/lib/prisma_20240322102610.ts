//The prisma.ts file is used to create an instance of the Prisma Client. That instance
// will be imported into the parts of the application that need to interact with the database.

import { PrismaClient } from '@prisma/client';

const prisma 