/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

/* @layer components {
Here you can add your own components and styles and apply them to your HTML with the specifiec class
 .btn {
    @apply bg-blue-500 text-white font-bold py-2 px-4 rounded;
  } 
} */

.h-screen-minus-nav {
  height: calc(100vh - 64px);
}
